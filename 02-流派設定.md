# 流派設定與待覆核清單

## 為什麼要把流派抽出來

同一個生辰排出兩張不同的盤，差異永遠出在少數幾個開關上。把它們集中成
`SchoolConfig` 一個物件，換來三件事：

1. 任何一張盤都可以連同它的設定一起存檔，日後完整重現。
2. `POST /chart/compare` 可以把兩個流派逐宮 diff。客戶問「點解你排出嚟同某網站唔同」，
   直接給答案，不用逐顆星肉眼對。
3. 你改師承定論時，改的是一張表，不是滿地程式碼。

## 開關總覽

| 開關 | 選項 | 影響範圍 |
|---|---|---|
| `year_boundary` | 正月初一 / 立春 | 年干支 → 祿存、四化、魁鉞、年支系全套 |
| `zi_rule` | 23 時換日 / 夜子時不換日 | 農曆日 → 紫微 → 十四正曜全套 |
| `use_true_solar_time` | 是 / 否 | 時辰 → 命宮、身宮、昌曲、空劫 |
| `leap_month_rule` | same / next / split | 月 → 命宮、身宮、左輔右弼 |
| `sihua_table` | tongxing / lineage | 生年四化、飛化 |
| `kuiyue_table` | tongxing / gengxin_mahu | 天魁天鉞 |
| `huoling_table` | quanshu / yinyang | 火星鈴星 |
| `daxian_age_basis` | 虛歲 / 實歲 | 大限起訖 |

## ⚠ 待你覆核的資料（依重要性排序）

系統目前所有流派表的 `_VERIFIED` 都是 `false`，用的是通行本流傳版本。
這不是偷懶 —— 這幾張表正是你師承資產所在，我不應該替你決定。
`strict_tables=True` 時，任何未覆核的表都會拋錯而不是默默用下去；
生產環境建議打開，逼自己把表補完。

### 1. 生年四化表（最高優先）

`tables.SIHUA_LINEAGE` 目前是空殼。爭議集中在兩干：

- **庚干**：通行本作「太陽祿、武曲權、太陰科、天同忌」。
  另有作天府化科者，亦有作天同化科、太陰化忌者。
- **壬干**：通行本作「天梁祿、紫微權、左輔科、武曲忌」。另有作天府化科者。

四化錯一格，整張盤的論斷方向就換了。填法：把 10×4 的 tuple 填入 `SIHUA_LINEAGE["table"]`，
`_VERIFIED` 改 `True`，然後 `SchoolConfig(sihua_table="lineage")`。

### 2. 廟旺利陷表

`app/ziwei/data/brightness.json`，22 顆星 × 12 宮 = 264 格，全部未覆核。
前端把亮度直接畫成透明度，所以這張表錯了，整張盤的視覺權重就錯了。

建議做法：一顆星一顆星覆核，覆核完就把星名加進 `verified_stars` 陣列。
`apply_brightness(strict=True)` 只會套用已覆核的星，其餘留白 ——
留白比印一個你沒點過頭的字好。

### 3. 火星鈴星起宮

分歧有兩層：起宮本身各家不同；以及一律順行（全書）還是陽男陰女順、陰男陽女逆。
目前預設全書版一律順行。

### 4. 天魁天鉞（庚干）

「甲戊庚牛羊」vs「庚辛逢馬虎」。兩張表都已備好，切 `kuiyue_table` 即可。

### 5. 雜曜補全

目前只安了台輔、封誥、天刑、天姚、三台、八座、恩光、天貴、紅鸞、天喜、天哭、天虛、
龍池、鳳閣、華蓋、咸池。若你論命會用到天巫、天月、陰煞、解神、蜚廉、破碎、
孤辰、寡宿、天官、天福、天廚，補在 `chart.py` 對應的 `*_stars()` 函式。

## 論斷規則放哪

排盤是公開演算法，寫成程式碼。**論斷規則不是。**

沿用你八字系統的做法：規則進 `app/rules/`，以 rules-as-code 編碼，永不外送 LLM。
`LLM_POLISH_ENABLED` 預設 `false`；即使開啟，也只潤飾已經由規則引擎產出的文字，
不參與任何推算。這條界線在 `main.py` 就守住 —— LLM 拿不到 `Chart` 物件。
